steps:
- checkout: self
  fetchDepth: 2
  submodules: true
- script: |
    brew update
    git clone https://bitbucket.org/blaze-lib/blaze/src/master/
    cd master
    cp -r ./blaze /usr/local/include
    cd ../
    mkdir build
    cd build
    cmake ..
    make -j 8
    bin/UnitTests